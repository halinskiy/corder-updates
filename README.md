# corder-updates

Sparkle appcast for [halinskiy/Corder](https://github.com/halinskiy/Corder).

Auto-published from `Scripts/release.sh` in the main repo.

The active feed URL is hard-coded into the app's `Info.plist`:
`https://halinskiy.github.io/corder-updates/appcast.xml`.
